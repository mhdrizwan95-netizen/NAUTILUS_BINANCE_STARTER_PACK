{
  "config_issues": 4,
  "container_risks": [
    "Legacy Deck runtime removed in favour of Ops command center",
    "Core trading services lack resource limits"
  ],
  "env_vars": {
    "OPS_API_TOKEN": "required_secret",
    ".env": "ignored_in_build"
  }
}
